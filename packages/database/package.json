{
  "name": "@repo/database",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "db:migrate:create": "drizzle-kit generate",
    "migrate:status:preview": "wrangler d1 migrations list test1 --preview --remote",
    "migrate:apply:preview": "wrangler d1 migrations apply test1 --preview --remote",
    "migrate:status:prd": "wrangler d1 migrations list test1 --remote",
    "migrate:apply:prd": "wrangler d1 migrations apply test1 --remote",
    "lint": "eslint --max-warnings=0",
    "lint:fix": "eslint --fix",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "0.33.0"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "drizzle-kit": "0.24.2",
    "typescript": "5.5.4",
    "wrangler": "3.73.0"
  },
  "volta": {
    "extends": "../../package.json"
  }
}
